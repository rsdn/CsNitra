# RecoveryImprovementPlan — execution checklist

Plan: `docs/RecoveryImprovementPlan.md` (waves 0 → 7).
Skill: `plan-execution`. One subagent per sub-point, sequential, verify (build + tests) then commit.

Status: `[ ]` not started · `[~]` in progress (exactly one) · `[✅]` done · `[❌]` failed (stop + explain).

## Wave 0 — measurement base (D1, D2-core)
- [✅] 0.1 D1: benchmark corpus — 7 scenarios (D1.1 100+ small errors; D1.2 one error at end of large file; D1.3 error in nested loops / beginning of iteration; D1.4 regex terminals; D1.5 ambiguous longest-match; D1.6 dirty file errors every N lines; D1.7 garbage + broken next construct — "double damage"). Time asserts on recovery per file; baseline before/after each wave.
- [✅] 0.2 D2-core: expose counters `RecoveryPasses`, `EngineGenerateCalls` in the benchmark report (visible per scenario).

## Wave 1 — contract and bottom: "always to the end + all errors"
- [✅] 1.1 A1: S6 "guaranteed progress" last-resort candidate (rank 6, generated always when E<EOF incl. snapshot==null; absorber [E..S) to known-good point or EOF; Ref→memo-patch, loop-level→loop absorber, else Absorb injection; snapshot==null→synthetic start-rule frame).
- [✅] 1.2 A5-7: S6 = panic bottom (stop-set = terminators (FOLLOW-union over stack, fallback rule-level) ∪ anchor-First; no snapshot requirement; no MaxSkip; strict regions excluded C1).
### 1.3 A4-2: "report and continue" (sub-points 1.3.1–1.3.6)
- [✅] 1.3.1 S6 node shape — `GenerateS6` memo-patch now shape-preserving (SeqNode/ListNode → natural Kind + absorber in `RawElements`; TerminalNode → `SomeNode`; null → absorber root). S6BottomTests 5/0, SeparatedListTests 20/0, full ParserTests 338/0/2. Commit 77cf522.
- [✅] 1.3.2 S6 guaranteed bottom — S6 (rank 6) exempt from the per-point budget; `MaxRecoveryIterations` 64→1000. D1.1 → `Success@EOF`; I6 preserved. Full ParserTests 338/0/2. Commit 77cf522-followup.
- [✅] 1.3.3 Recovery tests assert reached-EOF — CSharpGrammarTests `AssertFails` → `AssertRecoversWithEnd` done in 1.5 (11 raw + 7 regular/verbatim). ParserTests recovery tests audited: the 11 that don't assert reached-EOF are all mechanism-in-isolation (candidate generation, snapshot, follow/first sets, cost model, budget reachability, etc.) — correctly no reached-EOF assertion. ParserTests 338/0/2.
- [✅] 1.3.4 Unrecovered emission — when S6 (rank 6) is the accepted fallback candidate (the loop breaks on the first progress-giving candidate, so S6 accepted ⇒ no S0–S5 gave progress), emit `RecoveryDiagnostic(Kind.Unrecovered, E, …)` at the recovery point E. Gate is `candidate.Rank == 6` — NOT a budget heuristic (`attempts.Count > MaxRecoveryAttemptsPerPosition`). `UnrecoveredTests` (4): S6-fallback trailing garbage (snapshot==null) → ≥1 Unrecovered at E, budget-independent (3/16/1000); S1 repair accepted → no Unrecovered. Full ParserTests 342/0/2.
- [✅] 1.3.5 Strict regions — SUPERSEDED by 1.5 (C1): strict regions no longer `FatalError`; they emit only S6 (reach `Success@EOF` with an `Unrecovered` diagnostic). Covered by `Test_S6_Only_Generated_In_Strict_Region` (1.5). The old "strict ⇒ FatalError" contract was the explicit tension resolved in C1.
- [✅] 1.3.6 Acceptance tests — D1.1 (done in 1.3.2); D1.2 (3+ errors one unsavable → all reported + `Success@EOF` + ≥1 `Unrecovered`); D1.3 (strict region → `Success@EOF` + `Unrecovered`, per C1). `A42AcceptanceTests.cs`. ParserTests 344/0/2. Commit bb2159e.
- [✅] 1.4 A5-4: bottom-contract of result — CsNitra consumer accepts `Partial@EOF` (whole-input tree + diagnostics) instead of throwing `InvalidOperationException` (CsNitraParser.cs:147). A3 `RecoveryProfile` half (IDE vs Compiler result semantics, `Unrecovered`/`InsufficientStack` on tree) deferred to Wave 4.1. Solution builds; ParserTests 344/0/2. Commit 7dcac5d.
- [✅] 1.5 C1: "strict ⇒ only S6" — `Generate` strict gate suppresses S1–S5 but still emits S6 (absorber-to-EOF); `GenerateS6` absorber starts at `Math.Max(parseEnd, currentStartPos)`. 11 raw inputs reach `Success@EOF`. `Test_S6_Only_Generated_In_Strict_Region` (strict ⇒ only S6); re-scoped `Test_Recoverable_False_Disables_Epsilon_Acceptance` (ε-match disabled, S6 bottom still reaches EOF — distinguished via tree nodes). Also converted 7 pre-existing-failing `Regular`/`Verbatim` `AssertFails` (1.3.2 regression). ParserTests 338/0/2, CSharpGrammarTests 1518/0/3. Commit 2856aef. Unblocked 1.3.3.
- [✅] 1.6 C2: Partial-after-recovery semantics — verified S6 gives `Success@EOF` (not `Partial`); `Partial@EOF` is a distinct, intentional final state (postfix path reached EOF with holes, no recovery applied, `ErrorInfo==null`, empty diagnostics). No code change needed. `PartialAfterRecoveryTests.cs` (2). ParserTests 346/0/2.

## Wave 2 — re-parse speed (B1, A2)
- [✅] 2.1 B1: exact hygiene — `HygieneCore` removes only the start-rule record at `currentStartPos` + `Failure` with `currentStartPos <= pos <= e`; `Success`/`Partial` never removed (I2). `HygieneRemovals` test hook + `HygieneTests` (long file, removals bounded). ParserTests 347/0/2. Commit f65ff66.
- [✅] 2.2 A2: budgets by tiers — per-TIER sub-budgets (S1=4/S2=2/S3-S6=2); "attempt" = tier; S0/S6 exempt (S6 guaranteed fallback). `MaxRecoveryAttemptsPerPosition` kept as legacy no-op. Regression: large FollowSet → S3 still tried. Param in `RecoveryProfile` deferred to 4.1. ParserTests 348/0/2. Commit 01e4356.

## Wave 3 — "nothing matched" class (R1)
- [ ] 3.1 R1: garbage terminal (Roslyn `BadToken` analogue) — built-in `Garbage`, `TryMatch(p)` = "no terminal of the set matched at p", consumes to first position where someone matches or limit (64 chars / 16 tokens). Local absorber, not explosive S2/S3/S6.

## Wave 4 — profiles and predictability (A3, B4 + A4-5)
- [✅] 4.0 M: ParseSubRule — parse a rule without top-level recovery. DONE (27b4964). Test 2: 10k blocks 240s → 4.121s. `ParseExpressionLength` on 1000 blocks = 7 (constant, greedy raw-string — bug Q).
- [✅] 4.0-q Q: RawString regex absorbs 3+ quotes (two adjacent raw strings merge). DONE (1b3e46e). Hand-written `RawStringTerminal` (open run N>=3, scan to first close run >= N). `"""abc""" """def"""` → two literals; `"""a""b"""` → one. Test 2 (10k blocks): 4.155s → 14.8s (under 15s but at the edge — H needed urgently). CSharpGrammarTests 1524/0/3, ParserTests 359/0/2, CsPreprocessorTests 128/0/0.
- [✅] 4.0a H: HygieneCore — O(region), not O(memo). DONE (3732ea7). `_memoByPos` (Dictionary<int, HashSet<key>>) + IndexMemoAdd/Remove. Call sites: OnMemoWritten, OnMemoRemoved, RollbackPatches, Parse, ParseSubRule. Scaling: exponent 1.84→1.753 (O(region) remains). HygieneRemovals unchanged. All tests green.
- [✅] 4.0-noRec: no-recovery build fix — DONE (6ebb38c). `InQuietZone` + `RecoveryDiagnostics` stub added to `Parser.NoRecovery.cs`. Audit: no other partials missing. CI step added. Both builds 0 errors.
- [✅] 4.0-t T: ComputeSpans — cheap extent + IsInsideSpan bitmap. DONE (180c959 T1, a8a2816 T2+bitmap). Linear scan for ordinary/verbatim/raw; `RawString().TryMatch` for raw; `bool[]` bitmap for O(1) IsInsideSpan. Test 2: 14.8s → 4.5s. TimeBudget back to 15s (337992c). T3 not needed (0 interpolated in corpus).
- [✅] 4.1 A3: `RecoveryProfile` — DONE (0614097). Mode, MaxIterations, S1/S2/S3S6TierBudget, MaxSkip, MaxParseDepthBase/PerChar, StrategyMask (TODO). Convenience properties. ParserTests 362/0/2.
- [✅] 4.2 A4-5.1: Remove `#if RECOVERY`, delete NoRecovery — DONE (2ba05b7). 45 files, 47 changed. One build, depth-guard active in all modes.
- [✅] 4.2 A4-5.2: Bail mode (`Mode.Compiler`) in `Recover` — DONE (00b8bab). Break after first failure. RecoveryPasses==1, EngineGenerateCalls==0.
- [✅] 4.2 A4-5.4: Remove `EnableRecovery`/`DefineConstants` from `Directory.Build.props`, remove CI step — DONE (00b8bab). No functional references remain.
- [ ] 4.2 A4-5.3: Two-stage (consumer) — [deferred, NEEDS-SPEC: real compiler consumer].
- [✅] 4.3 B4.1: TimeBudget + degradation table — DONE (9d2558c). TimeBudget (500ms default, Test=Infinite), DegradationLevel record, Degradation.Levels (4 levels), Get with clamping.
- [✅] 4.3 B4.2: mechanism (DegradationLevel + Stopwatch + time-check + effective props) — DONE (c2990fe). EffectiveStrategyMask/MaxSkip/Speculation/ForceS6. ParserTests 369/0/2.
- [✅] 4.3 B4.3: mask + MaxSkip + speculation in RecoveryEngine — DONE (e97e12c). Generate gates S1-S5 by EffectiveStrategyMask (S6 floor not gated); GetMaxSkip fallback → EffectiveMaxSkip (frame override preserved, both sites); S2 speculation gated. ParserTests 373/0/2.
- [✅] 4.3 B4.4: hard limit (level 3: force S6 + stop) — DONE (2ab693c). ForceS6 branch in Recover; ForcedDegradationLevel test hook. Level 0 passes=3, level 3=1. ParserTests 374/0/2. B4 COMPLETE.
- [✅] 4.3 B4 bugfix: Test profile degraded instantly (Infinite=-1ms made `Elapsed>TimeBudget` always true). Guard `TimeBudget>TimeSpan.Zero` + Timeout.InfiniteTimeSpan. DONE (8251499). ParserTests 375/0/2.
- [ ] 4.4 P: Parse phase model (Normal vs Recovery) — split `Recover` into explicit phases (Normal / Recovery / Normal again) via `_inRecoveryMode` flag; `ReportMismatch`/`CaptureSnapshot` gate on the flag. Unblocks A4-4 п.1, A5-8, A4-1. Deferred: preventive (unblocks ~3% perf on 5a.5.2); after A3 may take `RecoveryProfile.Mode` form; after A4-5 simpler (no `#if RECOVERY`).

## Wave 5 — cheap candidates, boundaries, exact FOLLOW
### 5a (cheap)
- [✅] 5a.1 B2: spec-parse cache for whole `Recover` — `SpeculativeCache` wrapper (5a.1.1, 104b38a) + `_specCache` field/accessor/counters/`Reset` in `Recover` (5a.1.2, 9198973) + `GenerateS2` switched to `parser.SpecCache` (5a.1.3, 3b54000). Test input needs an `Ident` after the garbage to drive `Speculative` (else S3 wins). ParserTests 351/0/2.
- [✅] 5a.2 B3: cheap S2/S3 scans — trivia jump в S2/S3 (по всему trivia, вкл. комментарии — решение (б)); min-IndexOf для multi-char `Literal` в S2 (прыжок к вхождению); IndexOf в S3 не делаем (счётчик вложенности). Подзадачи: 5a.2.1 ✅ (8a12226) → 5a.2.1a ✅ (6f7c8e1) → 5a.2.2 ✅ (a264dfe) → 5a.2.3 ✅ (056b54b) → 5a.2.4 ✅ (657f792) → 5a.2.4a ✅ (64c0d7c, jump только при чисто multi-char Literal First). Правило тестовых входов: паддинг/комментарий строго после `e`, внутри `[e, maxS]`. ParserTests 358/0/2.
- [❌] 5a.3 A4-1: "quiet zone" — NEEDS-SPEC: 5a.3.1 ✅ (a995e88, state/hook/activation), 5a.3.2/5a.3.3 сняты (артефактный сценарий ненаблюдаем в текущей архитектуре). A4-1 → Wave 6 как метрика.
- [✅] 5a.4 A4-3: single-token deletion as rank-1 candidate — 5a.4.1 ✅ (915c514, `RecoveryKind.Extraneous`) + 5a.4.2 ✅ (b8c90e3, `GenerateS1b` between S1 and S2, absorber `[e..e+len)`, `len = pos - e`). Test: `"aab"`, `S := 'a' 'b'` → `Extraneous` diagnostic, Success@EOF. ParserTests 359/0/2.
- [~] 5a.5 A4-4: First-prefix filter + expected at boundary — **закрыта частично** (✅ 5a.5.1, ❌ 5a.5.2/5a.5.3). 5a.5.1 ✅ (cf65967, `_firstCache` + `ReferenceComparer` — безвредно, пригодится позже). 5a.5.2 ❌ NEEDS-SPEC (засташена): First-префильтр ломает recovery-семантику (snapshot зависит от frame-side-effects пробуемых альтернатив → 21 recovery-тест) + перф-выигрыш отрицательный (D1.2: 30.84→31.76ms, ratio 1.030). Требует фазовой модели (пункт P, Wave 4.0). 5a.5.3 ❌ NEEDS-SPEC: `_expected` очищается в `ReportMismatch` при `pos > ErrorPos`; запись при выходе из цикла теряется. Место — 5b/Wave 7 (A5-6/A4-7).
### 5b
#### 5b.1 A5-6: per-call-site FOLLOW (sub-points 5b.1.1 → 5b.1.2 → 5b.1.3, sequential)
- [✅] 5b.1.1 Core `GetTerminatorsPerSite(stack)` on `FollowSetCalculator` (instance method, uses `_rules`; no `Rule[]` param) — DONE (6787228). `GetTerminatorsPerSite`+`TailOf`+`SafeFollow` (+57 lines); `FollowSetPerSiteTests` 5/5; ParserTests 380/0/2. **Loop bug found in 5b.1.3 → fixed in 5b.1.1a.**
- [✅] 5b.1.1a (d2774e6) **FIX (bug from 5b.1.1):** `TailOf` had no `LoopFrame` branch → a loop frame fell to `SafeFollow(enclosingRule)` = `follow(Body)` (over-inclusion, e.g. `b`). Fix: for a `LoopFrame`, take the element index from the `SeqFrame` directly below (altIdx from the `RuleFrame` below that — **no `Parser.cs` change needed**), return `First(loopBody) ∪ First(remaining)`, `nullable = IsNullable(remaining)`. Required for correctness (the rule-level fallback for a loop is semantically wrong, not just less-precise). Walk the stack; per-call-site `follow_site(i)` per §9 formula. **Three tail cases (not two):** (1) `ei < last`: `First(tail) ∪ (nullable(tail) ? follow_site(i-1) : ∅)`; (2) `ei == last` (tail empty): `follow_site(i) = follow_site(i-1)` — **NOT the empty set** (most common case: last Seq element); (3) frame below not `RuleFrame` of same rule / production not `Seq` → rule-level fallback. **Seq per-site**; Loop/TDOPP (`PostfixFrameLocation`)/ContextScope/RuleFrame/unresolved → rule-level fallback (`GetFollowSet(R)`). (Loop per-site DEFERRED: `LoopFrameLocation` carries only `(LoopKind, Iteration)`, no element index — per-site `whatFollows` needs a `Parser.cs` enhancement, out of scope for one-file 5b.1.1; Loop stays at current rule-level = no regression.) AltIdx of `SeqFrame(R,ei)` from the `RuleFrame(R)` directly below; `tail = _rules[R][altIdx].Elements[ei+1..]`. Override `Options.Terminators ?? follow_site(i)` per-frame (folded in); `Recoverable:false` → rule-level follow; base {EOF}; order inner-first, EOF last. Test: hand-built stacks — `List := Ref(X) ',' Ref(X)` vs `Block := Ref(X) '}'` (follow_site(X) in List has `,`, NOT `}`); nested nullable tail; tail-empty case; TDOPP passthrough.
- [✅] 5b.1.2 Wire into `GetTerminators` (S3 stop-set): `GetTerminators` delegates to `GetTerminatorsPerSite` (stable interface for S6). Update `FollowSetTests.cs:669-780` to per-site. DONE (9eb83a8): 1-line delegation; FollowSetTests updated (each change justified); `Test_GetTerminators_ListPerSite` wiring check; ParserTests 381/0/2. **STOP-IF:** subagent must explain in a diff comment, for EACH changed test, why per-site is more correct than the old rule-level — else stop/report (fitting to a new result is not updating, it is losing coverage). Regression List/Block on a real snapshot; full ParserTests.
- [✅] 5b.1.3 (b016103) `Follow(top)` for S1: `GenerateS1` (RecoveryEngine.cs:94) `calc.GetFollowSet(top.RuleName)` → `calc.GetTerminatorsPerSite(snapshot.Stack)`. Test: S1 candidate for List has `,`, NOT `}`.
- [ ] 5b.1.4 (superseded by 5b.1.1a) Loop per-site — the element index is in the `SeqFrame` below the `LoopFrame` (no `Parser.cs` change); implemented by 5b.1.1a. Residual non-Seq-embedded loops stay rule-level fallback.
- [✅] 5b.2 A5-7 (S6 panic bottom) — already satisfied by Wave 1 (1.1/1.2). S6 consumes `GetTerminators`, now per-site (5b.1.2), so the S6 stop-set is automatically per-site. No re-work; verified by green suite (S6BottomTests, A42AcceptanceTests).
- [✅] 5b.3.1 (3a5be5d) A5-5 SoftSeparator option on `SeparatedList` (declaration): `SoftSeparator(Terminal? = null)` in the primary constructor + carried through `InlineReferences`.
- [✅] 5b.3.2 (86c0fb9) A5-5 SoftSeparator behavior: `ParseSeparatedList` (Parser.cs) — on separator mismatch, if the next token matches `SoftSeparator`, consume it + one `RecoveryKind.Skipped`, continue (no S2/S3). No-op when `SoftSeparator` null.
- [~] 5b.4 A5-1: bounded speculative probe — **PAUSED after 5b.4.1** (user re-prioritized to Wave 6/7 queue). 5b.4.1 ✅ (2e9642f): `RecoveryOptions.SoftDepth`(=2) + `DeriveProbePredicates` (top-frame Ref + loop-body Refs; exclude pure-regex-First/TDOPP/ContextScope; dedup internal-first; 7 tests). 5b.4.2–5b.4.5 pending. (a) S2 T2 `CanStart` derived from snapshot frames (via `Speculative` + specCache B2); (b) T1 anchors also for Ref frames. Accept = consumed ≥K tokens (K=2 default, `RecoveryOptions.SoftDepth`). Depth limit = work ceiling, not accept condition. Predicate set limited to loop-body Refs + top frame (not any Ref). T2 collection cap — first K positions per predicate (sort by Pos). Exclude pure regex-First (`Identifier`) predicates. TDOPP/ContextScope frames not derived. Order deterministic: author anchors first, then by frame proximity top→bottom. Depends on A2+B2+B3+A5-6. D1.7 acceptance test; S3 not silenced (regression).
- [ ] 5b.5 R2: stop predicate from expected set — S3/S6 stop scan at first position matching expected continuation (First-sets of structures expected by higher rule at E ∪ terminators).

## Wave 6 — diagnostics and metrics
- [✅] 6.1 A5-2: node-attached diagnostics via side-table (COMPLETE; variant C withdrawn per user decision). 6.1.1 ✅ (21a4e3f): pure structure-based `DeriveDiagnostics(root, input)` in `DiagnosticDerivation.cs` (superseded for the public list by the side-table; kept until 6.1.2c). 6.1.2 (side-table + reference identity; metadata in `RecoveryDiagnostic`, NOT the node → closes D1/D2/D4): 6.1.2a ✅ (0ef6642): side-table on `Parser` — `Dictionary<WeakReference<ISyntaxNode>, RecoveryDiagnostic[]>` + ref-identity comparer (`WeakRefNodeKeyComparer`; `ConditionalWeakTable`/`WeakReference<T>.Target` unavailable in netstandard2.0, CS0246/CS1061), fresh per `Parse` + `AttachDiagnostic` + `DeriveRecoveryDiagnostics` (1 file `Parser.Recovery.cs` + 1-line `Parse` init), 3 tests; 6.1.2b ✅ (1d2df3c): session-end match `FinishRecovery`→`AttachDiagnosticsToTree`→`MatchesRecoveryNode` (position+shape: region→`IsAbsorber` node, zero→zero-width node w/ matching `Terminal.Kind`) in `Parser.Recovery.cs` — all 7 strategies, D1 (`Extraneous`)/D4 (`Terminal`/`RuleName`); 6.1.2b2 ✅ (78d5380): soft-separator (D2) → `Parser.cs` (`ParseSeparatedList`); 6.1.2c ✅ (a797238): public `RecoveryDiagnostics` = `DeriveRecoveryDiagnostics(tree)` + `Unrecovered` at `FinishRecovery`→`FinalizeRecoveryDiagnostics`; 12 tests pass (no weakening). All 4 loss classes (D1/D2/D3/D4) closed (side-table stores exact accumulated instances). Stop-if: `ConditionalWeakTable` unavailable (netstandard2.0) → `Dictionary<WeakReference<ISyntaxNode>,…>` + reference comparer; `RecoveryDiagnostic`/`Generate` signature change → split; D3 text not reproduced → report. 6.1.3 (subtree-query API) → Wave 8 (IDE).
- [✅] 6.2 D2 (full): counters. 6.2.1 ✅ (d787970): `RecoveryMetrics` (per-S accept/rollback `int[7]` by rank, read-through specCache hits/misses from `SpeculativeCache`, reserved hygiene/time fields) + `Parser.Metrics`; accept/rollback wired in `TryCandidate` (keyed by `candidate.Rank`); 2 tests. 6.2.2 ✅ (64badff): time-by-phase (MainParse/Generation/Reparse, Stopwatch) + hygiene removals (HygieneRemovalsAfter) + braking-scenario test.
- [✅] 6.3 A4-6: grammar quality channel. 6.3.1 ✅ (8e82114): `GrammarDiagnostics` type (anchor usage, recovery-points/rule, strict-region firings; accumulates across parses) + `Parser.GrammarDiagnostics` + observation-only collection at 4 sites; 3 tests. 6.3.2 ✅ (d5267f0): `GetFindings` (5 kinds: AnchorNeverUsed, AnchorHeavilyUsed, RuleRecoveryPointsIdentical, StrictRegionNeverFires, S2AnchorRedundant) + `DeclaredSets` + D1-corpus-slice test.
- [~] 6.4 H: moved to Wave 4.0a (see 4.0a) — hygiene O(region) indexing, urgent (K×M quadratic), not Wave 6 audit.

## Wave 7 — polish
- [x] 7.1 R3: depth-guard calibration. 7.1.1 ✅ (0d54358): `MaxParseDepthCap`=400 (stack-safety) + check-before-increment + per-frame `EnsureSufficientExecutionStack` (>20) + `GuardFired()` bounded-work fix (limit-- per firing, O(cap²)/session — fixes the 6-test hang) + `MaxParseDepthReached` hook; DepthGuardTests (350-level no-crash + shallow). 7.1.2 ✅: guard-fired result = A5-4 bottom contract (Success@EOF, verified no recovery change) + one `InsufficientStack` diagnostic (added to `RecoveryKind`; `_guardFired`/`_guardFiredPos` first-firing flag; emitted result-level like `Unrecovered`). DepthGuardTests: 350-level → Success + 1 InsufficientStack; control (50-level) → Success + 0.
- [ ] 7.2 A5-3 / R5: diagnostics on first word of dirty region. 7.2.1: absorber/missing region `[E..S)` → first non-whitespace "word" (reuse `RecoveryEngine.cs:712` preview). 7.2.2: S2 insertion → first token; "end of previous line" for missing on new line (R5).
- [ ] 7.3 B5: clustering of expensive points — after a point that spent many attempts/time (threshold from profile), next iterations in same "dirty" region start immediately with coarse tier (S3/S6), skipping S2. D1.6 time grows linearly, not quadratically.
- [ ] 7.4 A5-5: SoftSeparator for `SeparatedList` — option `SoftSeparator(t)` — foreign separator at separator position consumed with `Skipped` diagnostic and treated as needed. Declarative (grammar-driven), not lambda.
- [ ] 7.5 A4-7: exact expected in final message — `FatalError`/`Unrecovered` expected = top frame `Expected` ∪ `GetTerminators(stack)` (per-site after A5-6) ∪ First of suffix obligations (S2/S4 mechanics → shared function).
- [ ] 7.6 A5-8 (optional): K-candidates in S3 — not `break` on first stop point, but K nearest (3–5) as separate candidates.
- [ ] 7.7 E: structurally for code generation — keep `RecoveryEngine.Generate` clean; `Injection` + `MemoPatch` only patch units (do not complicate); document "re-pass of prefix is cheap" property; do not introduce global chart.
- [ ] 7.8 S: Span input API — parser input as a span/range (not the whole file); separate from M and P, not urgent.

## Deviations
- **Design decision (user):** `Recoverable = false` removed from `ParseContextScope` (commit 89040dc). Rationale: any rule must be recoverable, including context-scoped ones (raw interpolated string literals); a broken parse tree is acceptable. `ParseContextScope` bodies now parse with default (recoverable) options, so S1–S5 repair candidates are available inside them (not just S6). The C1 strict-region gate in `RecoveryEngine.Generate` is kept for other `Recoverable:false` sources (e.g. `RecoveryRule`). This supersedes the "strict context ⇒ hard-fail" premise (plan §3.9) and narrows the C1 "strict ⇒ only S6" contract to non-ContextScope strict regions.
- 1.5: (a) Converted 7 `Regular`/`Verbatim` `AssertFails` → `AssertRecoversWithEnd` (beyond the 11 raw inputs) and deleted the unused `AssertFails` helper in `InterpolatedStringTests.cs`. These 7 were failing at the 1.3.2 baseline (pre-existing 1.3.2 S6 regression, verified by stashing) and reach EOF, so `AssertFails` was wrong; conversion was required to meet the "CSharpGrammarTests 0 failed" verification. Overlaps 1.3.3. (b) `Test_Recoverable_False_Disables_Epsilon_Acceptance` left red (second gate: recovery point `e=18` has no `Recoverable:false` frame ⇒ S6 fires and drives to EOF; pre-existing 1.3.2, not C1). Reported, not fixed, per task instruction. ParserTests = 1 failed (this test) / 337 passed / 2 skipped.
- 1.2: pure verification + tests (no production change — 1.1's S6 already satisfied all 4 A5-7 requirements). `}`-in-string scan is NOT string-aware (documented baseline, stops at in-string `}`); R1/B3 (Waves 3/5) will refine.
- 1.1: `ApplyPatches` reordered to Hygiene-before-Apply (S6 needs it); this made S2/S3 `PatchMemo` a no-op → real regression + stack overflow (NotPredicate cycle no longer broken). Fixed by `PatchMemo` creating a prec-0 key when none exist. S6 absorber starts at `parseEnd` (not `e`) to cover the actual trailing region when `ErrorPos > parseEnd`. Full suite 336/0/2.
- 0.1+0.2 combined into one subagent (tightly coupled: the D1 corpus report surfaces the D2-core counters). Done together in `RecoveryCorpusTests.cs`.
- D1.1 (120 errors) baseline: Success@EOF=False (120 > MaxRecoveryIterations=64), passes=65. This is the Wave-1 acceptance metric (A4-2/A3). D1.7 baseline resync=11 (Wave-5 A5-1 acceptance metric).
- D1.3/D1.4/D1.7 tests set `MaxRecoveryAttemptsPerPosition=16` (test-only instance setting, established pattern) so S2/S3 are reached within budget.
- Pre-existing `Nitra.sln` build-artifact modification (VS version stamp + Shared.projitems) excluded from Wave-0 commit (was modified before execution started).
